import { Link } from '@tanstack/react-router';

const About = () => {
    return (
        <div>
            <h1>About Page</h1>
            <Link to="/">Go to Home</Link>
        </div>
    );
};

export default About;